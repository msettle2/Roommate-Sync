<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link href="https://fonts.googleapis.com/css2?family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&display=swap" rel="stylesheet">
    <style>
        body{
            height: 100vh;
            font-family: "Roboto", sans-serif;
            background-color: #FFF2E2;
        } 
        .container{
            height: 100%;
        }
        .navbar {
            background-color: #FFF2E2 !important; 
        }

        .navbar h1 {
            font-size: 1.7rem;
        }

        .btn-custom, .btn-custom:hover {
            background-color: #64A5CA;
            color: white;
        }

        .sign-up-btn {
            padding:0.8rem;
        }

        .sync-header {
            color: #64A5CA; 
        }

        .login-form {
            background-color: white;
            padding: 3rem;
            border-radius: 10px;
            box-shadow: 0px 5px 10px rgb(212, 211, 211);    
        }

        .btn-form-submit {
            width:100%; 
        }

        input[type="email"],
        input[type="password"] {
            background-image: url('/images/user-logo.png'); 
            background-repeat: no-repeat;
            background-position: calc(100% - 10px) center;
            background-size: 20px 20px;
            border: 1px solid #6f6f6f;
            border-radius: 5px;
            padding: 5px 1rem;
            margin-bottom: 1.3rem;
        }

        input[type="password"] {
            background-image: url('/images/password-logo2.png'); 
        }

        nav img {
            margin-right: 10px;
        }

        .btn-custom a {
            text-decoration: none;
            color: inherit;             
        }


    </style>
</head>
<body>
    <main>
        <nav class="navbar bg-body-tertiary mt-3 mx-2">
            <div class="container-fluid">
                <div class="d-flex justify-content-between w-100">
                    <a class="navbar-brand" href="/">
                        <h1 class="d-flex align-items-center">
                            <img src="/images/sync-logo.png" alt="logo" width="40" height="40" class="d-inline-block align-text-top mr-5">Roommate&nbsp;<span class="sync-header">Sync</span>
                        </h1>
                    </a>
                    <a href="/users/new"><button type="button" class="btn btn-custom sign-up-btn">Sign Up</button></a>
                </div>
            </div>
        </nav>
        <div class="container d-flex align-items-center justify-content-center mt-4">
            <div class="login-form">
                <h2 class="text-center">Login</h2>
                <p>Enter your account details to sign in</p>
                <form action='/users/login' method = 'POST'>
                    <div class="mb-3">
                        <input type="email" class="form-control" id="email" name="email" required placeholder="Email">
                    </div>
                    <div class="mb-3">
                        <input type="password" class="form-control" id="password" name="password" required placeholder="Password">
                    </div>
                    <button type="submit" class="btn btn-custom btn-form-submit" value="Login">Submit</button>
                </form>
                <p class="mt-3">Don't have an account? <a id="newAccount" href="/users/new">Register</a></p> 
                <% if(errorMessages.length > 0) {%>
                    <div class="notification-container">
                        <% errorMessages.forEach(message=>{%>
                            <div class="notification alert">
                                <strong><%= message %></strong>
                            </div>
                            <%});%>
                    </div>
                <%}%>
                <% if(successMessages.length > 0) {%>
                    <div class="notification-container">
                    <% successMessages.forEach(message=>{%>
                        <div class="notification success">
                            <strong><%= message %></strong>
                        </button>
                        </div>
                        <%});%>
                    </div>
                <%}%>    
            </div>
            <div>
                <img src="/images/roommates.jpeg" class="img-fluid" alt="roommates">
            </div>
        </div>
       
    </main>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>